---
phase: 11-analytics
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - apps/web/src/components/analytics/WinLossPatterns.tsx
  - apps/web/src/components/analytics/RepCoachingCards.tsx
  - apps/web/src/components/analytics/ForecastView.tsx
  - apps/web/src/app/(dashboard)/analytics/layout.tsx
autonomous: true
requirements:
  - ANLT-01
  - ANLT-02
  - ANLT-03
  - ANLT-04

must_haves:
  truths:
    - "Win/loss patterns page displays signal-based patterns (stakeholder engagement, meeting cadence) alongside existing patterns"
    - "Rep coaching page shows email and meeting activity metrics per rep compared to top performer baseline"
    - "Forecast page shows engagement score column alongside confidence score"
    - "Analytics section has a shared layout with navigation between win-loss, coaching, and forecast pages"
  artifacts:
    - path: "apps/web/src/components/analytics/WinLossPatterns.tsx"
      provides: "Updated pattern cards rendering signal-based patterns"
    - path: "apps/web/src/components/analytics/RepCoachingCards.tsx"
      provides: "Updated rep cards showing emailsPerDeal and meetingsPerDeal metrics"
    - path: "apps/web/src/components/analytics/ForecastView.tsx"
      provides: "Updated forecast table with engagement score column"
    - path: "apps/web/src/app/(dashboard)/analytics/layout.tsx"
      provides: "Shared analytics layout with tab navigation"
  key_links:
    - from: "apps/web/src/components/analytics/WinLossPatterns.tsx"
      to: "/api/v1/analytics/win-loss"
      via: "Client-side fetch for time range changes"
      pattern: "fetch.*analytics/win-loss"
    - from: "apps/web/src/components/analytics/RepCoachingCards.tsx"
      to: "/api/v1/analytics/rep-coaching"
      via: "Server component data fetch"
      pattern: "fetch.*analytics/rep-coaching"
    - from: "apps/web/src/components/analytics/ForecastView.tsx"
      to: "/api/v1/analytics/forecast"
      via: "Server component data fetch"
      pattern: "fetch.*analytics/forecast"
---

<objective>
Update the three analytics frontend components to display the new signal-based metrics from the upgraded services, and add a shared analytics layout with tab navigation for cohesive UX.

Purpose: The backend services (Plan 01 and 02) add new data fields (signal-based patterns, emailsPerDeal, meetingsPerDeal, avgEngagementScore, signalContext). The frontend must surface this data to make the analytics meaningful to managers and leadership.

Output: Updated analytics components and shared layout.
</objective>

<execution_context>
@C:/Users/ClayCampbell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ClayCampbell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From Plan 01 service outputs (additive to existing interfaces):

win-loss.ts -- no interface changes, just more pattern entries in the patterns array. Existing WinLossPattern interface already handles them.

rep-coaching.ts -- additive fields:
```typescript
// Added to RepMetrics
emailsPerDeal: number;
meetingsPerDeal: number;

// Added to TopPerformerBaseline
avgEmailsPerDeal: number;
avgMeetingsPerDeal: number;
```

forecasting.ts -- additive field:
```typescript
// Added to ForecastStage
avgEngagementScore: number;  // 0.0 to 1.0
```

From Plan 02 service outputs:
```typescript
// Added to NextBestAction
signalContext: string | null;
```

Existing component file paths:
- apps/web/src/components/analytics/WinLossPatterns.tsx (client component)
- apps/web/src/components/analytics/RepCoachingCards.tsx (client component)
- apps/web/src/components/analytics/ForecastView.tsx (client component)

Existing page paths:
- apps/web/src/app/(dashboard)/analytics/win-loss/page.tsx
- apps/web/src/app/(dashboard)/analytics/rep-coaching/page.tsx
- apps/web/src/app/(dashboard)/analytics/forecast/page.tsx

UI libraries available: shadcn/ui (Card, Badge, Button, Table, Tabs), Tailwind CSS v4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analytics layout and update WinLossPatterns and ForecastView components</name>
  <files>apps/web/src/app/(dashboard)/analytics/layout.tsx, apps/web/src/components/analytics/WinLossPatterns.tsx, apps/web/src/components/analytics/ForecastView.tsx</files>
  <action>
**layout.tsx**: Create a shared layout for the analytics section that provides tab navigation between the three analytics pages. Use Next.js layout pattern with `children` prop.

```tsx
// Server component layout
import Link from "next/link";
// Use the usePathname hook pattern: create a client NavTabs component
```

Create a `NavTabs` client component inline or as a separate small component. Use shadcn Tabs or simple styled nav links:
- "Win/Loss" links to `/analytics/win-loss`
- "Rep Coaching" links to `/analytics/rep-coaching`
- "Pipeline Forecast" links to `/analytics/forecast`

Highlight the active tab based on `usePathname()`. Style with Tailwind: horizontal tabs at the top, border-b, active tab gets border-b-2 border-primary. Wrap `{children}` below the tabs.

**WinLossPatterns.tsx**: The existing component already renders patterns from the `patterns` array. Since Plan 01 adds new patterns as additional entries in the same array (using the existing WinLossPattern interface), the component will render them automatically. However, make these improvements:

1. Add pattern category icons/colors: Map `pattern.label` to visual indicators:
   - "Deal size" -> dollar icon or indigo badge
   - "Deal velocity" -> clock icon or blue badge
   - "Engagement depth" -> activity icon or green badge
   - "Stakeholder engagement" -> users icon or purple badge (new)
   - "Meeting cadence" -> calendar icon or orange badge (new)
   - "Response time" -> mail icon or teal badge (new)

   Use Lucide icons (already in the project): `DollarSign`, `Clock`, `MessageSquare`, `Users`, `Calendar`, `Mail`. Import from `lucide-react`.

2. Update the pattern card Badge to use the category color instead of the default outline variant.

3. Sort patterns by the absolute deviation from overall win rate (most interesting first) -- this may already be done by the service, but ensure the component doesn't re-sort in a way that breaks the order.

**ForecastView.tsx**: Update the stage breakdown table to show engagement scores:

1. Add `avgEngagementScore?: number` to the `ForecastStage` interface in the component.

2. Add a new table column "Engagement" between "Historical Rate" and "AI Confidence":
   ```tsx
   <TableHead className="text-right">Engagement</TableHead>
   ```

3. Render engagement score as a label with color coding:
   - Score 0.0: "None" (gray)
   - Score 0.01-0.3: "Low" (red)
   - Score 0.31-0.6: "Moderate" (yellow)
   - Score 0.61-1.0: "High" (green)

   Show the label and the underlying score: `High (0.8)` with the color class applied.

4. Handle `avgEngagementScore` being undefined (backward compatibility if service hasn't been upgraded yet) -- just show "N/A" in gray.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Analytics section has a shared layout with tab navigation. WinLossPatterns shows category icons/colors for each pattern type including new signal-based patterns. ForecastView shows engagement score column. All components handle missing new fields gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Update RepCoachingCards with signal-based metrics</name>
  <files>apps/web/src/components/analytics/RepCoachingCards.tsx</files>
  <action>
Update RepCoachingCards to display the new emailsPerDeal and meetingsPerDeal metrics from the upgraded rep-coaching service.

1. Add to `EnrichedRep` interface:
   ```typescript
   emailsPerDeal?: number;
   meetingsPerDeal?: number;
   ```
   Make them optional for backward compatibility.

2. Add to `TopPerformerBaseline` interface:
   ```typescript
   avgEmailsPerDeal?: number;
   avgMeetingsPerDeal?: number;
   ```

3. Update the **Top Performer Baseline** card to show 6 stats instead of 4 (use a responsive grid: `grid-cols-2 sm:grid-cols-3 lg:grid-cols-6`):
   - Existing: Win rate, Notes/deal, Tasks/deal, Days to close
   - New: Emails/deal, Meetings/deal
   - Only show new stats if the values are defined (not undefined).

4. In each **RepCard**, add email and meeting activity below the existing stats section. Show as a compact row:
   ```tsx
   <div className="flex gap-4 text-sm text-muted-foreground">
     {rep.emailsPerDeal !== undefined && (
       <span>{rep.emailsPerDeal.toFixed(1)} emails/deal</span>
     )}
     {rep.meetingsPerDeal !== undefined && (
       <span>{rep.meetingsPerDeal.toFixed(1)} meetings/deal</span>
     )}
   </div>
   ```

5. The deviation rows already render from `rep.deviations` array. The service now adds email/meeting deviations to this array, so they will render automatically using the existing `DeviationRow` component. No changes needed there.

6. Handle the case where emailsPerDeal and meetingsPerDeal are undefined (workspace has no signal data) -- just don't render those metrics.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>RepCoachingCards displays email and meeting activity metrics per rep. Top performer baseline shows 6 metrics. New signal-based deviations render automatically via existing DeviationRow component. Backward compatible when signal data is absent.</done>
</task>

</tasks>

<verification>
1. `cd apps/web && npx tsc --noEmit` -- zero type errors
2. `pnpm build` -- full build succeeds
3. Analytics layout provides tab navigation between all three pages
4. All components render without errors when receiving data with or without the new fields
</verification>

<success_criteria>
- Analytics section has cohesive navigation between win-loss, coaching, and forecast pages
- Win/loss patterns show visual category indicators for signal-based patterns
- Rep coaching shows email and meeting activity metrics per rep vs baseline
- Forecast table includes engagement score column
- All components degrade gracefully when signal data fields are absent
- Full build passes with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/phase-11/11-03-SUMMARY.md`
</output>
