---
phase: 09-workflow-automation-collab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(dashboard)/settings/automations/page.tsx
  - apps/web/src/components/automations/automation-rule-form.tsx
  - apps/web/src/components/automations/automation-rules-list.tsx
  - apps/web/src/components/automations/condition-row.tsx
  - apps/web/src/db/schema/automations.ts
  - apps/web/src/services/automation-rules.ts
autonomous: true
requirements:
  - WKFL-01
  - WKFL-02
  - WKFL-03
  - WKFL-04
  - WKFL-05

must_haves:
  truths:
    - "User can create an automation rule by selecting a trigger, defining conditions, and choosing an action via a form UI"
    - "Automation rules list shows enable/disable toggles and last-triggered timestamps"
    - "Disabled rules do not fire when matching signals arrive"
    - "Triggers include stage_changed, record_created, email_received, meeting_ended, note_added"
    - "Actions include enqueue AI generate, send email, create task, create note"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/settings/automations/page.tsx"
      provides: "Automation rules settings page"
      min_lines: 40
    - path: "apps/web/src/components/automations/automation-rule-form.tsx"
      provides: "Form-based trigger-condition-action rule creator"
      min_lines: 100
    - path: "apps/web/src/components/automations/automation-rules-list.tsx"
      provides: "Rules list with toggle and last-triggered display"
      min_lines: 60
    - path: "apps/web/src/services/automation-rules.ts"
      provides: "Service layer for rule CRUD with lastTriggeredAt tracking"
      exports: ["listRules", "createRule", "updateRule", "deleteRule", "toggleRule"]
  key_links:
    - from: "apps/web/src/components/automations/automation-rule-form.tsx"
      to: "/api/v1/automations"
      via: "fetch POST/PATCH"
      pattern: "fetch.*api/v1/automations"
    - from: "apps/web/src/components/automations/automation-rules-list.tsx"
      to: "/api/v1/automations"
      via: "fetch GET + PATCH for toggle"
      pattern: "fetch.*api/v1/automations"
    - from: "apps/web/src/services/automation-rules.ts"
      to: "apps/web/src/db/schema/automations.ts"
      via: "Drizzle queries"
      pattern: "automationRules"
---

<objective>
Build the form-based automation rule builder UI and rules management page.

Purpose: Users need to create, edit, enable/disable, and monitor automation rules without code. The backend schema and API already exist -- this plan builds the UI and adds a lastTriggeredAt column for monitoring.

Output: Settings > Automations page with rule form (trigger/condition/action), rules list with toggles, last-triggered timestamps.
</objective>

<execution_context>
@C:/Users/ClayCampbell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ClayCampbell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/web/src/db/schema/automations.ts
@apps/web/src/app/api/v1/automations/route.ts
@apps/web/src/app/api/v1/automations/[id]/route.ts
@apps/web/src/services/automation-engine.ts
@apps/web/src/lib/job-queue.ts
@apps/web/src/app/(dashboard)/settings/layout.tsx

<interfaces>
<!-- Existing automation schema the UI builds against -->

From apps/web/src/db/schema/automations.ts:
```typescript
export const automationActionEnum = pgEnum("automation_action", [
  "enqueue_ai_generate", "enqueue_email_send", "enqueue_email_sync",
  "enqueue_calendar_sync", "create_task", "create_note",
]);

export const automationRules = pgTable("automation_rules", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull(),
  name: text("name").notNull(),
  enabled: boolean("enabled").notNull().default(true),
  triggerType: text("trigger_type").notNull(),
  conditions: jsonb("conditions").notNull().default([]),
  actionType: automationActionEnum("action_type").notNull(),
  actionPayload: jsonb("action_payload").notNull().default({}),
  createdBy: text("created_by"),
  createdAt: timestamp("created_at").notNull().defaultNow(),
  updatedAt: timestamp("updated_at").notNull().defaultNow(),
});
```

From apps/web/src/app/api/v1/automations/route.ts:
```typescript
// GET /api/v1/automations -> lists rules for workspace
// POST /api/v1/automations -> creates rule (requires admin)
// PATCH /api/v1/automations/[id] -> updates rule fields
// DELETE /api/v1/automations/[id] -> deletes rule
```

Trigger types used in automation-engine.ts:
- "stage_changed" (payload: { newStage, oldStage })
- "record_created" (payload: { objectType })
- "meeting_ended" (payload: {})
- "note_added" (payload: { noteText })
- "email_received" (payload: { fromEmail, text })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lastTriggeredAt column and automation rules service</name>
  <files>apps/web/src/db/schema/automations.ts, apps/web/src/services/automation-rules.ts</files>
  <action>
1. Add `lastTriggeredAt` column to `automationRules` table in the schema:
   ```typescript
   lastTriggeredAt: timestamp("last_triggered_at"),
   ```
   This is nullable (null = never triggered).

2. Run `pnpm db:push` to apply the schema change.

3. Create `apps/web/src/services/automation-rules.ts` with these functions:
   - `listRules(workspaceId: string)` - SELECT all rules for workspace, ordered by createdAt desc
   - `createRule(workspaceId: string, createdBy: string, input: { name, triggerType, conditions, actionType, actionPayload })` - INSERT with validation
   - `updateRule(workspaceId: string, ruleId: string, updates: Partial<{name, triggerType, conditions, actionType, actionPayload, enabled}>)` - UPDATE with workspace scope check
   - `deleteRule(workspaceId: string, ruleId: string)` - DELETE with workspace scope
   - `toggleRule(workspaceId: string, ruleId: string, enabled: boolean)` - UPDATE enabled field only
   - `markTriggered(ruleId: string)` - UPDATE lastTriggeredAt = NOW()

4. Add Zod validation schemas in the service file:
   - `conditionSchema`: z.object({ field: z.string(), operator: z.enum(["equals", "not_equals", "contains", "greater_than", "less_than"]), value: z.unknown() })
   - `createRuleSchema`: z.object with name (min 1, max 100), triggerType enum, conditions array, actionType enum, actionPayload object
   - Validate trigger types: "stage_changed", "record_created", "email_received", "meeting_ended", "note_added"

All queries must be scoped by workspaceId. Follow the pattern in apps/web/src/services/notifications.ts.
  </action>
  <verify>
    <automated>cd apps/web && npx tsx -e "import { listRules } from './src/services/automation-rules'; console.log(typeof listRules)"</automated>
  </verify>
  <done>Service exports all 6 functions. Schema has lastTriggeredAt column. Zod schemas validate rule inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Build automation rule form and rules list UI</name>
  <files>apps/web/src/components/automations/automation-rule-form.tsx, apps/web/src/components/automations/automation-rules-list.tsx, apps/web/src/components/automations/condition-row.tsx, apps/web/src/app/(dashboard)/settings/automations/page.tsx</files>
  <action>
1. Create `apps/web/src/components/automations/condition-row.tsx`:
   - A row component with 3 fields: field name (text input), operator (select dropdown with equals/not_equals/contains/greater_than/less_than), value (text input)
   - "Remove" button (X icon) to delete the condition
   - Uses shadcn/ui Select and Input components

2. Create `apps/web/src/components/automations/automation-rule-form.tsx`:
   - Props: `onSubmit(rule)`, `initialValues?` (for edit mode), `onCancel()`
   - Form sections:
     a. **Name**: text input, required
     b. **Trigger**: select dropdown with options:
        - "Stage Changed" (value: stage_changed)
        - "Record Created" (value: record_created)
        - "Email Received" (value: email_received)
        - "Meeting Ended" (value: meeting_ended)
        - "Note Added" (value: note_added)
     c. **Conditions**: list of ConditionRow components with "Add Condition" button. AND logic (all must match). Show helper text: "All conditions must match (AND logic)"
     d. **Action**: select dropdown with options:
        - "Generate AI Asset" (value: enqueue_ai_generate) - shows sub-field for documentType (opportunity_brief, proposal, deck, followup, battlecard)
        - "Send Email" (value: enqueue_email_send)
        - "Create Task" (value: create_task) - shows sub-field for task title
        - "Create Note" (value: create_note) - shows sub-field for note content
     e. **Action Payload**: dynamic fields based on selected action type
   - Submit button: "Create Rule" or "Update Rule" depending on mode
   - Use shadcn/ui Card, CardHeader, CardContent for section layout
   - Form validation: name required, triggerType required, actionType required

3. Create `apps/web/src/components/automations/automation-rules-list.tsx`:
   - Props: `rules[]`, `onToggle(id, enabled)`, `onEdit(rule)`, `onDelete(id)`, `loading`
   - Renders a list/table of rules with columns:
     - Name
     - Trigger type (human-readable label)
     - Action type (human-readable label)
     - Last Triggered (relative time like "2 hours ago" or "Never")
     - Enabled toggle (shadcn/ui Switch component)
     - Actions (Edit pencil, Delete trash)
   - Use shadcn/ui Switch for toggle, which calls onToggle immediately
   - Empty state: "No automation rules yet. Create your first rule above."

4. Create `apps/web/src/app/(dashboard)/settings/automations/page.tsx`:
   - Client component that manages state
   - Fetches rules from GET /api/v1/automations on mount
   - "New Rule" button opens the form (inline expand or dialog)
   - Uses AutomationRuleForm for create/edit
   - Uses AutomationRulesList for display
   - Toggle calls PATCH /api/v1/automations/[id] with { enabled: !current }
   - Delete calls DELETE /api/v1/automations/[id] with confirmation dialog (shadcn AlertDialog)
   - Edit pre-fills form with existing rule data

5. Add "Automations" link to the settings layout navigation in `apps/web/src/app/(dashboard)/settings/layout.tsx` -- add to the nav items array alongside existing items (Objects, AI, Integrations, etc.). Use a Zap icon from lucide-react.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Settings > Automations page renders with rule creation form (trigger/condition/action), rules list with enable/disable Switch toggles and last-triggered timestamps. Form validates required fields. All 5 trigger types and 4 action types are selectable.</done>
</task>

</tasks>

<verification>
1. Navigate to Settings > Automations -- page loads without errors
2. Create a rule: name "Auto proposal", trigger "Stage Changed", condition field "payload.to" operator "equals" value "Proposal", action "Generate AI Asset" with documentType "proposal"
3. Rule appears in list with "Never" for last triggered, toggle is ON
4. Toggle rule OFF -- switch updates, PATCH request sent
5. Edit rule -- form pre-fills with existing values
6. Delete rule -- confirmation dialog appears, rule removed after confirm
7. `pnpm build` passes with no TypeScript errors
</verification>

<success_criteria>
- Automation rules page accessible at Settings > Automations
- Users can create rules with all 5 trigger types and 4 action types
- Conditions support field/operator/value rows with AND logic
- Rules list shows enable/disable toggles (Switch) and last-triggered timestamps
- All CRUD operations work against existing /api/v1/automations endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/phase-09/09-01-SUMMARY.md`
</output>
