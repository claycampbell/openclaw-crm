---
phase: 06-infrastructure-ux-polish
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - apps/web/e2e/phase-06-infra.spec.ts
  - apps/web/e2e/phase-06-ux.spec.ts
autonomous: false
requirements: [INFR-01, INFR-02, INFR-03, INFR-04, UXPL-01, UXPL-02, UXPL-03, UXPL-04, UXPL-05, UXPL-06]

must_haves:
  truths:
    - "E2E tests verify the full signal-to-job execution pipeline"
    - "E2E tests verify toast notifications appear on record mutations"
    - "E2E tests verify pagination loads additional records on scroll"
  artifacts:
    - path: "apps/web/e2e/phase-06-infra.spec.ts"
      provides: "E2E test for job execution and signal pipeline"
    - path: "apps/web/e2e/phase-06-ux.spec.ts"
      provides: "E2E test for toasts, validation, and pagination"
  key_links:
    - from: "apps/web/e2e/phase-06-infra.spec.ts"
      to: "apps/web/src/app/api/v1/cron/jobs/route.ts"
      via: "API call to trigger job processing"
      pattern: "/api/v1/cron/jobs"
---

<objective>
Write E2E tests covering the Phase 6 infrastructure and UX changes, then run a human verification checkpoint to confirm the full phase works end-to-end.

Purpose: The project uses only Playwright E2E tests (no unit tests). This plan adds coverage for the job pipeline and UX polish features, then lets the user verify the visual and interactive elements.

Output: E2E test files, human verification of toast/validation/pagination behavior.
</objective>

<execution_context>
@C:/Users/ClayCampbell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ClayCampbell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-06/06-01-SUMMARY.md
@.planning/phases/phase-06/06-02-SUMMARY.md
@.planning/phases/phase-06/06-03-SUMMARY.md
@.planning/phases/phase-06/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write E2E tests for Phase 6 infrastructure and UX</name>
  <files>apps/web/e2e/phase-06-infra.spec.ts, apps/web/e2e/phase-06-ux.spec.ts</files>
  <action>
1. **Create `apps/web/e2e/phase-06-infra.spec.ts`** to test the job pipeline:
   - Test: Enqueue a job via API (POST to a test endpoint or direct DB insert), hit the cron endpoint (GET /api/v1/cron/jobs), verify the job status changed from pending to completed in the background_jobs table
   - Test: Enqueue a job that will fail (register a test handler that throws), hit cron, verify retries increment and status stays pending, hit cron 2 more times, verify status becomes failed (dead-letter)
   - Test: Create a deal record via API, verify a signal_events row is created for record_created, verify a background_jobs row is created with type signal_evaluate

2. **Create `apps/web/e2e/phase-06-ux.spec.ts`** to test UX polish:
   - Test: Navigate to an object records page, create a new record, verify a toast notification appears with success message (check for sonner toast element in DOM: `[data-sonner-toast]`)
   - Test: Navigate to create record dialog, submit with empty required fields, verify inline validation error messages appear (check for FormMessage elements)
   - Test: Navigate to a records page, verify the table loads with records, scroll down (if enough records exist), verify more records appear (pagination)
   - Test: If a delete action exists, click delete, verify AlertDialog appears with confirmation prompt

3. Follow existing E2E test patterns in `apps/web/e2e/`. Check what authentication setup is used (fixtures, beforeAll login). Match the same patterns.

4. Keep tests focused and fast. Use `test.describe` groups. Mark infrastructure tests that need DB access with appropriate tags.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:e2e --grep "phase-06" 2>&1 | tail -30 || echo "E2E tests written, run manually with: cd apps/web && pnpm test:e2e"</automated>
  </verify>
  <done>E2E test files exist covering job execution pipeline, toast notifications, form validation, and pagination. Tests follow existing project patterns.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 6 infrastructure and UX polish:
- Job execution engine (processJobs calls handlers, FOR UPDATE SKIP LOCKED, exponential backoff, dead-letter)
- Signal-to-automation pipeline (writeSignalEvent auto-enqueues signal_evaluate)
- Toast notifications on all mutations (sonner)
- Error boundaries on dashboard (react-error-boundary)
- AlertDialog for destructive actions (replaces browser confirm)
- Inline form validation (react-hook-form + Zod)
- Cursor-based pagination (replaces limit=200)
- Virtual scrolling (only visible rows rendered)
  </what-built>
  <how-to-verify>
1. Start the dev server: `pnpm dev`
2. Navigate to any object page (e.g., People or Deals)
3. **Toast test**: Create a new record -- verify a green success toast appears in bottom-right
4. **Validation test**: Open create record dialog, leave required fields empty, click submit -- verify red inline error messages appear below empty fields
5. **Pagination test**: If you have 50+ records in any object, scroll the table -- verify more records load as you scroll down (check network tab for cursor-based API calls)
6. **AlertDialog test**: Try to delete a record -- verify a styled confirmation dialog appears (not a browser confirm popup)
7. **Infrastructure test** (optional, requires DB access): Check background_jobs table after creating a deal -- verify signal_evaluate job was created
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. E2E tests exist and match project patterns
2. All 5 success criteria from ROADMAP Phase 6 are covered
3. Build passes: `pnpm build`
4. Human verification of toast, validation, pagination, and dialog behavior
</verification>

<success_criteria>
- E2E tests cover job pipeline, toasts, validation, and pagination
- Human confirms toasts appear on mutations
- Human confirms validation errors show inline
- Human confirms pagination loads more records on scroll
- Human confirms AlertDialog replaces browser confirm
- All Phase 6 requirements (INFR-01 through INFR-04, UXPL-01 through UXPL-06) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/phase-06/06-05-SUMMARY.md`
</output>
