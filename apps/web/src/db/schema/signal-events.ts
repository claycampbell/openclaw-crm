/**
 * Signal events stub — re-exported from signals.ts for backwards compatibility.
 * The canonical table definition is in signals.ts.
 */
export { signalEvents, processedSignals } from "./signals";
export type { } from "./signals";
